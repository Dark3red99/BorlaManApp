{
  "permissions": {
    "allow": [
      "Bash(xargs -I{} sh -c 'echo {}; cat {}/package.json 2>/dev/null | grep \"\\\\\"version\\\\\"\"')",
      "Bash(xargs grep -l \"id\")",
      "Bash(npx tsc *)"
    ]
  }
}
